mkdir /kiwi
linux32 kiwi --prepare $(pwd) --root /kiwi/mono-system --force-new-root
linux32 kiwi --type vmx --create /kiwi/mono-system --destdir /kiwi/images
linux32 kiwi --type iso --create /kiwi/mono-system --destdir /kiwi/images
