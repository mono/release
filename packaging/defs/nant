EXTERNAL_SOURCE=1

BUILD_HOSTS=(
	fedora-3-i386
)

USE_HOSTS=(
	ALL
)

# Should we build all this noarch stuff on the 1.1.13 bundle?
#bundle="1.1.13"

MONO_DEPS=(
	mono
)

get_destroot () {
	DEST_ROOT=noarch
}

BUILD_DIR="/tmp"

make_dist () {
	# wget doesn't work for some reason...
        curl -O http://easynews.dl.sourceforge.net/sourceforge/nant/nant-$VERSION-src.tar.gz
}

