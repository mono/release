#username=builder
#hostname=pbartok2.provo.novell.com

username=builder
hostname=t1000-mono.cam.novell.com
USE_ZIP_PKG=1
#env="PATH=/usr/local/bin:/usr/ccs/bin:/usr/sfw/bin:$PATH"
#env="PATH=/usr/local/bin:/usr/sfw/bin:/usr/ccs/bin:$PATH"

# Use blastwave instead... they better represent the stuff that comes with solaris10
#  Plus, sunfreeware doesn't have an updated binutils for solaris10 sparc
env="PATH=/opt/csw/bin:$PATH"

strip_path=/usr/ccs/bin/strip
tar_path=/usr/sfw/bin/gtar
