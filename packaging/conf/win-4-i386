username=builder
hostname=win-7-x86.lab.xamarin.com
USE_ZIP_PKG=1
# Cygwin's ssh server doesn't load this information
# ... let's do it explicitly through sshutils
target_command_prefix='source /etc/profile;'

web_index=5

# updated winnt.h from a new cygwin install (from Jonathan Chambers)

