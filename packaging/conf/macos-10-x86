username=builder
hostname=macos-10-x86.lab.xamarin.com
USE_ZIP_PKG=1

# Helps with relocating binaries
#  use -Xlinker so libtool doesn't drop the headerpad flag
env="LDFLAGS= -Xlinker -headerpad_max_install_names "

web_index=7

