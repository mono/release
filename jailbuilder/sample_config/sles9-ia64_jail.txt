# Format: <name>=<value>

jail_name=sles-9-ia64

#ftp://ftp.novell.com/pub/ximian/redcarpet2/sles-9-ia64/
rpm_repository_path=sles9-ia64rpms

# Only accept packages with these arches
valid_arch=ia64 noarch

required_rpms=gcc make autoconf automake vim rug iputils util-linux sles-release mktemp procps aaa_base aaa_skel sudo pwdutils

# Environment to set before executing things inside the jail (format: var=value var2=value2, etc...)
environment=

# Other various/optional post jail settings:
nameservers=130.57.22.5
users=builder


