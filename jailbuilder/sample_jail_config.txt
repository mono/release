# Format: <name>=<value>

jail_name=suse-93-i586

#rpm_repository_path=suse93rpms
rpm_repository_path=small_group_of_rpms
#rpm_repository_path=suse93rpms_x86_64

# Only accept packages with these arches
valid_arch=i386 i586 i686 noarch

required_rpms=aaa_base gcc make autoconf automake

#extra_external_rpms? (download and install inside jail: rcd/rug for example)

# Other various/optional post jail settings:
nameservers=nameserver 192.168.1.1
users=builder


